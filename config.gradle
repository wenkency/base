// 整个项目的配置
ext {
    // false : 组件模式（可以独立运行）
    // true  : 依赖模式
    isModule = false
    app = [
            compileSdkVersion: 32,
            minSdkVersion    : 14,
            targetSdkVersion : 32,
            versionCode      : 1,
            versionName      : "1.0",
    ]

    appId = [
            applicationId: "cn.boardour.app"
    ]
    // 公共配置
}