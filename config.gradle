// 整个项目的配置
ext {
    // false : 组件模式（可以独立运行）
    // true  : 依赖模式
    isModule = false
    app = [
            compileSdkVersion: 30,
            buildToolsVersion: "30.0.3",
            minSdkVersion    : 16,
            targetSdkVersion : 30,
            versionCode      : 1,
            versionName      : "1.0",
            baseVersionCode  : 1,
            baseVersionName  : "1.0",
    ]

    appId = [
            applicationId: "cn.boardour.app"
    ]
}